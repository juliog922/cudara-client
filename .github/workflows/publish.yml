name: CI/CD - Test & Publish

on:
  push:
    branches: [main]
    tags: ["v*"] # Trigger publish logic only on version tags
  workflow_dispatch:

env:
  PYTHON_VERSION: "3.12"
  CLIENT_DIR: "."

jobs:
  test:
    name: Run Unit Tests
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ${{ env.CLIENT_DIR }}
    steps:
      - uses: actions/checkout@v4

      - name: Setup uv
        uses: astral-sh/setup-uv@v7
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          enable-cache: true

      - name: Install dependencies
        run: uv sync --dev --locked

      - name: Run tests
        run: uv run pytest -v

  publish:
    name: Publish to PyPI
    runs-on: ubuntu-latest
    needs: [test]
    # Only run this job if a tag is pushed (prevents "File exists" errors on main branch commits)
    if: startsWith(github.ref, 'refs/tags/v')
    defaults:
      run:
        working-directory: ${{ env.CLIENT_DIR }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0 # Required for hatch-vcs to determine version from git history

      - name: Setup uv
        uses: astral-sh/setup-uv@v7
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          enable-cache: true

      - name: Build distributions
        run: uv build

      - name: Publish to PyPI
        env:
          UV_PUBLISH_TOKEN: ${{ secrets.PYPI_API_TOKEN }}
        run: uv publish